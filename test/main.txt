extern def log(x: f64): f64
extern def putchar(x: char): i64
extern def gets(buffer: &char): &char
extern def malloc(size: i64): &char
extern def free(ptr: &char)

struct Point {
    x: i64,
    y: i64
}

def triple(x: i64): Point {
    res = Point()
    res.x = x * 3
    return res
}

def main(): i64 {
    j = [1, 2, 3]
    v = malloc(10)
    gets(v)
    putchar(v[0])
    putchar(v[1])
    putchar(v[2])
    putchar(v[3])
    return 0
}
