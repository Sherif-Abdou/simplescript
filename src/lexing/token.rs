#[derive(Debug, PartialEq, Clone)]
pub enum Token {
    Identifier(String),
    Integer(i64),
    Float(f64),
    Def,
    Return,
    OpenCurly,
    ClosedCurly,
    OpenSquare,
    CloseSquare,
    OpenParenth,
    CloseParenth,
    Plus,
    Minus,
    Star,
    Slash,
    Ampersand,
    Greater,
    Lesser,
    GreaterEqual,
    LesserEqual,
    DoubleEqual,
    NotEqual,
    If,
    Else,
    Equal,
    Colon,
    Comma,
    EOL,
    EOF,
}